import React from "react";

interface ProjectProps {
    icon: string;
    tittle: string;
    description: string;
}

const Project = ({icon, tittle, description}: ProjectProps) => {

}

export default Project;